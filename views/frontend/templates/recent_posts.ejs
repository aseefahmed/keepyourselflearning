<!-- <div class="widget">
	<form>
		<div class="form-group">
			<input type="text" name="search" id="search" class="form-control" placeholder="Search...">
		</div>
		<button type="submit" id="submit" class="btn_1 rounded"> Search</button>
	</form>
</div> -->
<div class="widget">
	<div class="widget-title">
		<h4>Recent Posts</h4>
	</div>
	<ul class="comments-list">
		<% recents.forEach(function(recent){ %>
				<li>
					<div class="alignleft">
						<a href="/post/get/<%= recent._id %>"><img src="/img/uploads/<%= recent.image %>" alt=""></a>
					</div>
					<small><%= new Date(recent.created_at).toDateString() %></small>
					<h3><a href="/post/get/<%= recent._id %>" title=""><%= recent.title %></a></h3>
				</li>
		<% }) %>
		
	</ul>
</div>
<div class="widget">
	<div class="widget-title">
		<h4>Popular Posts</h4>
	</div>
	<ul class="comments-list">
		<% popular_posts.forEach(function(pop){ %>
				<li>
					<div class="alignleft">
						<a href="/post/get/<%= pop._id %>"><img src="/img/uploads/<%= pop.image %>" alt=""></a>
					</div>
					<small><%= new Date(pop.created_at).toDateString() %></small>
					<h3><a href="/post/get/<%= pop._id %>" title=""><%= pop.title %></a></h3>
				</li>
		<% }) %>
		
	</ul>
</div>